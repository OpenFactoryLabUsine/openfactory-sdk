Met à jour la DB UNS du Lab-Usine avec les données venant de OpenFactory.

Mettre le mdp pour la base de données dans un fichier .env (pour l'instant ça peut être n'importe quoi vu qu'on ne se connecte pas à la vraie base de données)

Exemple de fichier .env:
```
SQL_SA_PASSWORD=MotDePasse
```